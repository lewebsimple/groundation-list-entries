# groundation-list-entries

List entrypoints recursively from a directory.

## Usage

```
const ListEntries = require('groundation-list-entries');

const entries = ListEntries('./src/js', /\.js$/));
```
